<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.mkch.youshi">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".config.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/login_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
        </activity>
        <activity android:name=".activity.TestMsgActivity"/>
        <activity
            android:name=".activity.UserLoginActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"/>
        <activity
            android:name=".activity.UserRegPhoneActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.UserRegCodeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.UserRegUserNameActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.UserForgotCodeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.GroupChatActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.AddFriendsActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.NewFriendActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.PhoneContactsActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.FriendInformationActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.InformationSettingActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.RemarkInformationActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:name=".fragment.month.MonthActivity"
            android:screenOrientation="portrait">
        </activity>
        <activity android:name=".activity.TestNetActivity"/>
    </application>

</manifest>